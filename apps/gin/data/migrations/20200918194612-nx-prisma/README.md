# Migration `20200918194612-nx-prisma`

This migration has been generated by daevahush at 9/18/2020, 3:46:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
"hostedGameId" text   ,
"gameId" text   ,
"gameHand" text []  ,
"gameScore" integer   NOT NULL DEFAULT 0,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Game" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"hostId" text   NOT NULL ,
"deck" text []  ,
"discard" text []  ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.username_unique" ON "public"."User"("username")

CREATE UNIQUE INDEX "User_hostedGameId_unique" ON "public"."User"("hostedGameId")

ALTER TABLE "public"."User" ADD FOREIGN KEY ("hostedGameId")REFERENCES "public"."Game"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."User" ADD FOREIGN KEY ("gameId")REFERENCES "public"."Game"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200908184620-nx-prisma..20200918194612-nx-prisma
--- datamodel.dml
+++ datamodel.dml
@@ -3,23 +3,28 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
-  id        String   @id @default(cuid())
-  username  String   @unique
-  password  String
-  game      Game?    @relation(fields: [gameId], references: [id])
-  gameId    String?
-  gameHand  String[]
-  gameScore Int      @default(0)
+  id           String   @id @default(cuid())
+  username     String   @unique
+  password     String
+  hostedGame   Game?    @relation("host", fields: [hostedGameId], references: [id])
+  hostedGameId String?
+  game         Game?    @relation(name: "users", fields: [gameId], references: [id])
+  gameId       String?
+  gameHand     String[]
+  gameScore    Int      @default(0)
 }
 model Game {
   id      String   @id @default(cuid())
-  users   User[]   @relation
+  name    String
+  host    User     @relation(name: "host")
+  hostId  String
+  users   User[]   @relation(name: "users")
   deck    String[]
   discard String[]
 }
```


